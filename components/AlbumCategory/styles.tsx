import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginBottom: 10
    },
    title: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold',
        marginLeft: 10
    }
});